language: c
script:
  - make vars
  - DEBUG=1 make vars
  - make test/optimized
  - make clean
  - LSAN_OPTIONS=verbosity=1:log_threads=1 make test/ci
os:
  - linux
  - osx
compiler:
  - clang
  - gcc
notifications:
  email: false
after_success:
  - bash <(curl -s https://codecov.io/bash)
